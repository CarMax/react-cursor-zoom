/*
 * gulpfile.js
 * ===========
 * Each task has been broken out into its own file in build/tasks.
 * Any file in that folder gets automatically required by the loop in ./build/index.js (required below).
 *
 * To add a new task, simply add a new task file to build/tasks.
 */

require('./build');
